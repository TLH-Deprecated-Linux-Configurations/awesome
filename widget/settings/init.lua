return {
    require(... .. '.auto-start'),
    require(... .. '.bootup_configuration'),
    require(... .. '.lazy_load_boot'),
    require(... .. '.notifications'),
    require(... .. '.signals'),
    require(... .. '.state')
}
